public class Main {
    public static void main(String[] args) {
        Chain chain_instance = new Chain();
        chain_instance.overall_check(90);
        chain_instance.overall_check(-50);
        chain_instance.overall_check(0);
    }
}